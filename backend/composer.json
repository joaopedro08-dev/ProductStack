{
    "require": {
        "vlucas/phpdotenv": "^5.6"
    },
    "scripts": {
        "dev": "php -S localhost:8000 -t . routes/router.php --timeout=0",
        "start": "php -S localhost:8000 -t . routes/router.php --timeout=0"
    }
}